<UserControl x:Class="RegionContext.Views.PersonDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RegionContext.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Persion Details:"/>
        <TextBlock Text="{Binding SelectedPerson.FirstName}" FontSize="24" Margin="10"/>
        <TextBlock Text="{Binding SelectedPerson.LastName}" FontSize="24" Margin="10"/>
        <TextBlock Text="{Binding SelectedPerson.Age}" FontSize="24" Margin="10"/>
    </StackPanel>
</UserControl>
